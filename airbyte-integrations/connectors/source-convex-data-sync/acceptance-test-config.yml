# See [Connector Acceptance Tests](https://docs.airbyte.io/connector-development/testing-connectors/connector-acceptance-tests-reference)
# for more information about how to configure these tests
connector_image: airbyte/source-convex-data-sync:dev
test_strictness_level: low
acceptance_tests:
  spec:
    tests:
      - spec_path: "source_convex_data_sync/spec.yaml"
  connection:
    tests:
      - config_path: "secrets/config.json"
        status: "succeed"
      - config_path: "integration_tests/invalid_config.json"
        status: "failed"
  discovery:
    tests:
      - config_path: "secrets/config.json"
  # TODO: needs credentials for a deployment with the tables in integration_tests/configured_catalog.json.
  # basic_read:
  #   tests:
  #     - config_path: "secrets/config.json"
  #       configured_catalog_path: "integration_tests/configured_catalog.json"
  #       expect_records:
  #         path: "integration_tests/expected_records.jsonl"
  # The incremental suite's future-state test expects zero records for abnormal_state.json, but Convex answers an
  # unknown cursor with InvalidDataSyncCursor and this connector then fails with a configuration error (a cursor from
  # another deployment must not be silently restarted), so that test needs a bypass_reason when the suite is enabled.
  # incremental:
  #   tests:
  #     - config_path: "secrets/config.json"
  #       configured_catalog_path: "integration_tests/configured_catalog.json"
  #       future_state:
  #         future_state_path: "integration_tests/abnormal_state.json"
  #         bypass_reason: "An unknown cursor is rejected with a configuration error (InvalidDataSyncCursor) instead of returning no records."
  # full_refresh:
  #   tests:
  #     - config_path: "secrets/config.json"
  #       configured_catalog_path: "integration_tests/configured_catalog.json"
